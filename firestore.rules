rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Each account can only read and write its own caregiver doc, patients, medicines, and logs
    match /caregivers/{uid}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
